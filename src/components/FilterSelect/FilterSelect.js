import React from 'react';import PropTypes, { string } from 'prop-types';import { bindActionCreators } from 'redux';import { connect } from 'react-redux';import { setFilter } from '../../redux/actions/filterActions';const FilterSelect = ({ filterParam, type, setFilter }) => {  return (    <select name={type} onChange={(e) => setFilter(type, e.target.value)}>      {filterParam.map((param) => (        <option value={param}>{param}</option>      ))}    </select>  );};const mapDispatchToProps = {  setFilter,};FilterSelect.propTypes = {  filterParam: PropTypes.arrayOf(string).isRequired,};export default connect(null, mapDispatchToProps)(FilterSelect);